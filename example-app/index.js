/**
 * Module Dependencies
 */

var uid = require('uid');
var events = require('events');
var infinite = require('infinity');
var test = require('./test');

/**
 * UID
 */

console.log(uid(10));
console.log(infinite);
